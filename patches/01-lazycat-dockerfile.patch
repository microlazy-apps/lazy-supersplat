diff --git a/.dockerignore b/.dockerignore
new file mode 100644
index 0000000..8a11047
--- /dev/null
+++ b/.dockerignore
@@ -0,0 +1,10 @@
+.git
+.gitignore
+.github
+node_modules
+dist
+docs
+*.md
+.DS_Store
+.idea
+.vscode
diff --git a/Dockerfile b/Dockerfile
new file mode 100644
index 0000000..bf388ed
--- /dev/null
+++ b/Dockerfile
@@ -0,0 +1,33 @@
+# syntax=docker/dockerfile:1
+# Lazycat-wrapper Dockerfile for SuperSplat.
+# Upstream is a static SPA (rollup -> dist/), no backend. We build with
+# Node, then serve dist/ with nginx so we don't need to ship Node at runtime.
+
+# ---------- build stage ----------
+FROM node:22-alpine AS build
+WORKDIR /app
+
+# Install deps first to keep the layer cacheable across source-only changes.
+COPY package.json package-lock.json ./
+RUN npm ci --no-audit --no-fund
+
+# Build the SPA. BASE_HREF stays empty so emitted asset paths are relative
+# to "/", which is what nginx serves.
+COPY . .
+ENV BASE_HREF=""
+RUN npm run build
+
+# ---------- runtime stage ----------
+FROM nginx:1.27-alpine
+
+# Drop the default site config; our nginx.conf owns server { ... }.
+RUN rm -f /etc/nginx/conf.d/default.conf
+COPY nginx.conf /etc/nginx/conf.d/supersplat.conf
+
+COPY --from=build /app/dist /usr/share/nginx/html
+
+EXPOSE 80
+HEALTHCHECK --interval=30s --timeout=5s --start-period=10s --retries=3 \
+  CMD wget -qO- http://127.0.0.1/health || exit 1
+
+CMD ["nginx", "-g", "daemon off;"]
diff --git a/nginx.conf b/nginx.conf
new file mode 100644
index 0000000..d57ead2
--- /dev/null
+++ b/nginx.conf
@@ -0,0 +1,65 @@
+server {
+    listen 80 default_server;
+    listen [::]:80 default_server;
+
+    root /usr/share/nginx/html;
+    index index.html;
+
+    # The lazycat reverse proxy already terminates TLS and adds
+    # X-Forwarded-*; nothing to do here besides serve the static bundle.
+
+    sendfile on;
+    tcp_nopush on;
+    tcp_nodelay on;
+
+    gzip on;
+    gzip_vary on;
+    gzip_proxied any;
+    gzip_comp_level 6;
+    gzip_types
+        application/javascript
+        application/json
+        application/wasm
+        application/xml
+        text/css
+        text/javascript
+        text/plain
+        text/xml
+        image/svg+xml;
+
+    # Health probe target for lzc-manifest.health_check.
+    location = /health {
+        access_log off;
+        add_header Content-Type text/plain;
+        return 200 "ok\n";
+    }
+
+    # Service worker must always come from the network so a new release
+    # actually replaces the previous SW + cache (sw.ts version-stamps its
+    # cache key from package.json version).
+    location = /sw.js {
+        add_header Cache-Control "no-cache, no-store, must-revalidate";
+        try_files /sw.js =404;
+    }
+
+    # Same for the entry HTML — index.html references hashed asset paths,
+    # so the HTML itself must never be stale.
+    location = /index.html {
+        add_header Cache-Control "no-cache, no-store, must-revalidate";
+        try_files /index.html =404;
+    }
+
+    # Hashed bundle assets emitted by rollup + the /static tree (locales,
+    # icons, wasm libs) are content-addressed enough to cache for a year.
+    location ~* \.(?:js|css|map|wasm|png|jpe?g|webp|svg|woff2?|ttf|json)$ {
+        expires 1y;
+        add_header Cache-Control "public, immutable";
+        try_files $uri =404;
+    }
+
+    # SPA fallback — every other path serves the entry HTML.
+    location / {
+        add_header Cache-Control "no-cache, no-store, must-revalidate";
+        try_files $uri $uri/ /index.html;
+    }
+}
